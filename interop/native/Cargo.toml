[package]
name = "interop-native"
version = "0.0.0"
edition = "2021"
publish = false
description = "Writes and reads SQLCipher files with rusqlite's bundled SQLCipher"

[dependencies]
rusqlite = { version = "0.40", features = ["bundled-sqlcipher"] }
